{
  "name": "userscripts",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "@prisma/client": "^6.18.0",
      "prisma": "^6.18.0",
      "better-auth": "^1.3.34"
    }
  },
  "overrides": {
    "esbuild": "^0.25.11"
  },
  "scripts": {
    "auth:generate": "bunx @better-auth/cli generate --config packages/auth/index.ts --output packages/db/prisma/schema.prisma",
    "format": "bunx biome format --write"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.3.34",
    "@biomejs/biome": "2.3.2",
    "wrangler": "^4.45.2",
    "typescript": "^5.9.3"
  }
}
